# AI-Powered Running Training Dashboard — Architecture Plan

> Generated by Claude Code Opus 4, June 24 2026
> Cost: $0.29 | Turns: 1 | Session: c79cff36

> **SUPERSEDED (issue #292).** This is the original plan, kept for history. The
> title's "AI-Powered" framing, the Overview's "2026 AI-first frontend skills",
> and the Tech Stack entry "**AI**: Vercel AI SDK (streamUI, generative UI,
> provider routing)" are all obsolete. Strava's API Policy 2026 §5.3 forbids
> Strava data — including derived, anonymised or aggregated data — in any AI
> application, explicitly including "ingestion into a context window or working
> memory"; §5.10 forbids passing it to AI providers; §5.16(b) forbids
> MCP/agent-mediated interfaces that expose it. The model layer was therefore
> **deleted**, not gated behind a key, and must not be rebuilt in any form.
> §5 (AI Integration Architecture), the AI rows in §7 and the generative-UI lines
> in §1/§3/§4/§9/§10 describe an approach Stride no longer uses, and the
> "interview talking points" at the bottom must not be repeated as claims.
> Deterministic-first is what ships: `buildAnalysisInput` + `heuristicBlocks` in
> `lib/ai/analysis.ts`, the typed block contract in `lib/ai/tools.ts` rendered by
> `CoachFeed`, the rule engine in `lib/coach/*`, the training maths in
> `lib/training/*`, and the one readiness mapping in `lib/cobalt/readiness.ts`.
> `lib/ai/provider.ts`, `lib/ai/harmony.ts`, `lib/ai/prompts.ts`,
> `lib/ai/coach-tools.ts`, `app/api/ai/chat/` and `actions/chat.ts` no longer
> exist. Read `AGENTS.md` for the current architecture.

## Overview

A portfolio project demonstrating 2026 AI-first frontend skills. Next.js 16, Vercel AI SDK with generative UI streaming, Strava OAuth, and production-grade architecture.

## Tech Stack
- **Framework**: Next.js 16 (App Router, PPR, Turbopack)
- **Language**: TypeScript strict mode
- **Styling**: Tailwind CSS + shadcn/ui
- **AI**: Vercel AI SDK (streamUI, generative UI, provider routing)
- **Database**: Drizzle ORM + Vercel Postgres
- **Auth**: NextAuth.js v5
- **Visualization**: Recharts
- **Deployment**: Vercel

---

## 1. Project Structure

```
stride/
├── app/
│   ├── (marketing)/
│   │   ├── layout.tsx
│   │   └── page.tsx                    # "/"
│   ├── (app)/
│   │   ├── layout.tsx                  # Auth-gated shell
│   │   └── dashboard/
│   │       ├── page.tsx                # PPR shell
│   │       ├── loading.tsx
│   │       ├── error.tsx
│   │       └── activity/[id]/
│   │           ├── page.tsx
│   │           └── loading.tsx
│   ├── api/
│   │   ├── auth/[...nextauth]/route.ts
│   │   ├── strava/
│   │   │   ├── webhook/route.ts        # Edge
│   │   │   └── sync/route.ts           # Node
│   │   └── ai/
│   │       ├── analyze/route.ts        # Streaming AI
│   │       └── chat/route.ts           # Phase 2
│   ├── layout.tsx
│   └── globals.css
├── actions/                            # Server Actions
├── components/
│   ├── ui/                             # shadcn primitives
│   ├── marketing/
│   ├── dashboard/
│   ├── activity/
│   ├── ai/
│   │   ├── analysis-panel.tsx          # Orchestrates streaming
│   │   ├── insight-card.tsx            # Generative UI primitive
│   │   ├── trend-callout.tsx           # Generative UI primitive
│   │   ├── workout-recommendation.tsx  # Generative UI primitive
│   │   ├── metric-comparison.tsx       # Generative UI primitive
│   │   └── streaming-text.tsx
│   └── providers/
├── lib/
│   ├── auth.ts
│   ├── db/
│   ├── strava/
│   ├── ai/
│   │   ├── provider.ts                 # Provider router
│   │   ├── tools.ts                    # Generative UI tool registry
│   │   └── prompts.ts
│   ├── crypto.ts                       # AES-GCM encrypt/decrypt
│   └── metrics.ts
├── drizzle/
│   ├── schema.ts
│   ├── relations.ts
│   └── migrations/
├── hooks/
├── stores/
├── types/
│   ├── domain.ts
│   └── ai.ts
├── next.config.ts
├── middleware.ts
└── package.json
```

## 2. Database Schema (Drizzle + Vercel Postgres)

Current tables:
- **users** — linked to Strava athlete
- **strava_tokens** — AES-256-GCM encrypted OAuth tokens (GDPR)
- **activities** — running workouts from Strava with full metrics

> **WITHDRAWN (issue #292)** — the following three tables are **dropped**, not
> pending. They were removed in `drizzle/migrations/0009_milky_vampiro.sql`:
> - ~~**ai_analyses** — cached AI responses with `inputHash` deduplication~~
> - ~~**chat_messages** — Phase 2 RAG conversations~~
> - ~~**activity_embeddings** — Phase 2 pgvector with HNSW index~~
>
> None of them may be recreated. `activity_embeddings` in particular would put
> Strava activity data in a vector store, which §5.3 and §5.16(b) forbid. The
> `vector` extension is still created by `scripts/migrate.mjs` only so a fresh
> database can replay migration 0000's `vector(1536)` column; nothing reads it.

### Key security decisions:
- OAuth tokens encrypted at rest (AES-256-GCM, per-row IV)
- ~~`inputHash` deduplication — AI calls once per dataset, not per page view~~ (withdrawn in #292: there are no AI calls and no analysis cache)
- Raw Strava payload stored for audit/re-processing

## 3. Route Design

| Route | Type | Runtime | Purpose |
|-------|------|---------|---------|
| `/` | Server | node | Landing + Connect Strava |
| `/dashboard` | Server (PPR) | node | Static shell + streamed AI |
| `/dashboard/activity/[id]` | Server | node | Detail + on-demand AI |
| `/api/auth/[...nextauth]` | Handler | node | NextAuth v5 |
| `/api/strava/webhook` | Handler | **edge** | Strava webhook (≤2s response) |
| `/api/strava/sync` | Handler | node | Activity backfill |
| `/api/ai/analyze` | Handler | node | Streaming generative UI |
| `/api/ai/chat` | Handler | node | RAG chat (Phase 2) |

## 4. Component Architecture

### Server Components (no JS shipped)
- Marketing pages, auth shell, dashboard page, activity detail
- Stats header, activity list, splits table

### Client Components
- Interactive: activity-row, filters-bar, connect-strava-button
- Charts (Recharts): weekly-volume, pace-distribution, HR-zones, activity-map

### AI Components (Generative UI)
- **insight-card** — title, severity, body
- **trend-callout** — direction + metric + sparkline
- **workout-recommendation** — suggested session with rationale
- **metric-comparison** — this period vs last period

> **Crucial distinction:** The model doesn't generate React. It calls typed tools (`showTrend`, `showInsight`), and the SDK renders YOUR pre-defined components with validated props. Say this precisely in interviews.

## 5. AI Integration Architecture

### Provider Routing
Abstraction layer that selects primary model, falls back to secondary on failure. One swap point for all AI calls.

### Generative UI Flow (streamUI)
1. Server loads user's activities + computed metrics
2. `streamUI({ model, system, prompt, tools })` with Zod-validated tool definitions
3. Model interleaves streamed prose with tool-driven component renders
4. Mixed UI tree streams into `<analysis-panel>` with `<Suspense>` boundary

### Streaming Patterns
- `useObject` for structured analysis with known shape
- `streamUI` + tools for flexible generative UI
- `useChat` for Phase 2 RAG chatbot
- Independent `<Suspense>` boundaries: stats, charts, AI panel never block each other

### RAG Architecture (Phase 2)
1. On sync: generate NL summary → embed → store in pgvector (HNSW, cosine)
2. On query: embed → vector search (scoped to userId) → inject into system prompt
3. k=4-6 chunks, always filtered by userId (prevents cross-user leakage)

## 6. Data Flow

### Strava OAuth
```
Connect → /oauth/authorize (PKCE) → callback → exchange code →
encrypt tokens → backfill 30 activities → redirect to dashboard
```

### Webhook → Sync Pipeline
```
Strava POST → Edge webhook (enqueue, 200 within 2s) →
Node sync handler → refresh token → fetch activity →
map to domain model → upsert → revalidateTag("activities")
```

### AI Analysis Trigger
```
Open dashboard → requestAnalysis → compute inputHash →
check cache → HIT: return stored | MISS: streamUI →
persist summary + toolCalls → return
```

## 7. State Management (Non-overlapping)

| Layer | Tool | Holds |
|-------|------|-------|
| Server cache | TanStack Query | Activities, refetch, sync status |
| AI streaming | Vercel AI SDK | useObject, useChat streams |
| UI | Zustand | Theme, filters, selected activity |

> Server data never in Zustand. AI state never in Query. Clean separation is the senior signal.

## 8. Performance Strategy

- **PPR**: Static shell prerendered, dynamic content streams via Suspense
- **Suspense boundaries**: Independent boundaries for stats, charts, AI panel
- **Edge webhook**: Low-latency 200 response, heavy work deferred to node
- **Tag-based revalidation**: Event-driven (on sync), not time-based ISR
- **AI cost control**: `inputHash` caching — model called once per dataset

## 9. Phase 1 MVP Scope

### Build:
- Strava OAuth + encrypted token storage + refresh
- Activity sync (backfill 30 activities)
- Dashboard: stats, weekly volume chart, pace distribution, activity list
- Activity detail: map, splits, HR zones
- **One** AI flow: streaming generative-UI weekly insight (streamUI + 3-4 primitives)
- Polished empty/loading/error states
- **Demo mode**: seeded fixture data (hiring managers won't connect Strava)
- Deployed to Vercel

### Deliberately excluded:
- RAG chatbot
- Embeddings/pgvector
- Multi-user
- Predictive ML
- Training plan generation

## 10. Key Architectural Decisions

| Decision | Choice | Why |
|----------|--------|-----|
| ORM | Drizzle over Prisma | SQL-first, edge-compatible, faster cold starts, shows SQL fluency |
| Auth | NextAuth v5 over Clerk | Free, no vendor lock-in, demonstrates OAuth understanding |
| AI location | Server-side only | Keys never reach browser, controllable caching, GDPR boundaries |
| UI paradigm | Generative UI over plain text | Typed components, model on rails, 2026 AI-first signal |
| Caching | Event-driven over ISR | Running data changes on new activity only, not on timer |

---

## Notes for Benjamin

### Interview talking points:
1. **"I built generative UI"** — NOT "AI returns React." AI calls typed tools that render my pre-defined components.
2. **"Provider-routing abstraction with EU data residency awareness"** — NOT "I use DeepSeek because it's cheap."
3. **"OAuth tokens encrypted at rest with AES-256-GCM"** — GDPR competence signal.
4. **"AI calls are deduplicated via content hashing"** — cost awareness.

### Strava API warning:
Strava has app-review and rate limits. Build a **demo mode with fixture data** so the deployed URL works without OAuth.


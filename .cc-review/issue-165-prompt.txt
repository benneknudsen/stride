Du skal fikse issue #165 i stride-projektet.

### Mål
- Slet den døde komponent `components/ui/input.tsx`, da den ikke bruges nogen steder.

### Acceptkriterier
1. Slet filen `components/ui/input.tsx`.
2. Verificer at `npm run build`, `npm test -- --run --reporter=dot` og `npm run check` stadig fungerer.
3. `git add -A && git commit -m "chore(ui): slet død input-komponent (#165)"`

### Sted i koden
- `components/ui/input.tsx` — slet den.

### Constraints (fra AGENTS.md)
- Biome 2.5: `npm run check`.
- Type-check: `npx tsc --noEmit`.
- Test: `npm test -- --run --reporter=dot`.

### Vigtigt
- Koden må ikke bryde eksisterende tests.
- Gør kun ændringer der er nødvendige for at løse issue.

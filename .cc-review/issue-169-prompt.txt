Du skal fikse issue #169 i stride-projektet.

### Mål
- Begrænse beskedlængde og antal beskeder i chat-endpointens request-schema.

### Acceptkriterier
1. `chatMessageSchema.content` får `.max(4000)`.
2. `requestSchema.messages` får `.max(50)`.
3. Oversize requests afvises med 400 (Bad Request) eller 413 (Payload Too Large) — brug en passende HTTP-status.
4. Skriv test der dækker at et for langt indhold (4001+) og for mange beskeder (51+) afvises.
5. `git add -A && git commit -m "fix(coach): begræns chat-beskedlængde (#169)"`

### Sted i koden
- Filer: `app/api/ai/chat/route.ts` (ca. linje 62-68 for schema, linje 416-418 for persist).
- Test: `__tests__/chat/` eller tilsvarende.

### Constraints (fra AGENTS.md)
- TypeScript strict: ingen `any` uden begrundelse.
- Biome check: `npm run check`.
- Lint/format: `npm run lint` + `npm run format`.
- Type-check: `npx tsc --noEmit`.
- Test: `npm test -- --run --reporter=dot`.

### Vigtigt
- Koden må ikke bryde eksisterende tests.
- Brug allerede eksisterende test-setup, ikke nye frameworks.
- Gør kun ændringer der er nødvendige for at løse issue.

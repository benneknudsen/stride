Du skal fikse issue #163 i stride-projektet.

### Mål
- Fjern ubrugte eslint-devDependencies fra package.json.

### Acceptkriterier
1. Fjern `eslint` og `eslint-config-next` fra `devDependencies` i package.json.
2. Kør `npm install` for at opdatere package-lock.json.
3. Verificer at `npm run build`, `npm test -- --run --reporter=dot` og `npm run check` stadig fungerer.
4. `git add -A && git commit -m "chore(deps): fjern ubrugte eslint-devDependencies (#163)"`

### Sted i koden
- `package.json:61-62` (devDependencies).
- Intet andet skal ændres.

### Constraints (fra AGENTS.md)
- Biome 2.5: `npm run check`.
- Type-check: `npx tsc --noEmit`.
- Test: `npm test -- --run --reporter=dot`.

### Vigtigt
- Koden må ikke bryde eksisterende tests.
- Gør kun ændringer der er nødvendige for at løse issue.

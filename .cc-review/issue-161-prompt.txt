Du skal fikse issue #161 i stride-projektet.

### Mål
- Ret default-varianten i button-komponenten til at bruge Cobalt-tokens i stedet for Volt-grøn.

### Acceptkriterier
1. `buttonVariants` default er `bg-cobalt text-silver hover:bg-cobalt-dim` (eller tilsvarende Cobalt-tokens).
2. Fjern Volt-grønne tokens fra `globals.css` hvis de ikke bruges andre steder.
3. Tjek at 404-siden (`app/not-found.tsx`) og fejl-siden (`components/dashboard/error-state.tsx`) ser korrekte ud.
4. Skriv test der verificerer at button default-varianten bruger Cobalt-tokens (ikke Volt-tokens).
5. `git add -A && git commit -m "fix(ui): ret default button-variant til Cobalt (#161)"`

### Sted i koden
- Filer: `components/ui/button.tsx:10` (default-variant), `app/not-found.tsx:16`, `components/dashboard/error-state.tsx:37`.
- CSS: `app/globals.css` (Volt-tokens).
- Test: `__tests__/ui/` eller tilsvarende.

### Constraints (fra AGENTS.md)
- TypeScript strict: ingen `any`.
- Biome 2.5: `npm run check`.
- Type-check: `npx tsc --noEmit`.
- Test: `npm test -- --run --reporter=dot`.
- Brand: Cobalt `#1b29c0`, Red `#ee2418`, Silver `#e9eae5`, Ink `#5560a8`. Tokens + `cg-*` utilities i `globals.css`.

### Vigtigt
- Koden må ikke bryde eksisterende tests.
- Gør kun ændringer der er nødvendige for at løse issue.
- Hvis Volt-tokens stadig bruges andre steder, så behold dem men ret default-varianten.

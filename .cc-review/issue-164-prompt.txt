Du skal fikse issue #164 i stride-projektet.

### Mål
- Parallelisér alle tre DB-reads i chat-ruten (getRacePlan, getDashboardActivities, getChatHistory).

### Acceptkriterier
1. `getChatHistory(userId, MAX_CONTEXT_MESSAGES)` skal inkluderes i samme `Promise.all` som `getRacePlan` og `getDashboardActivities`.
2. Brug `catch(() => [])` på `getChatHistory` (samme pattern som de to andre).
3. Opdater koden derefter til at bruge den parallelle variabel.
4. Verificer at `npm test -- --run --reporter=dot`, `npx tsc --noEmit` og `npm run check` stadig fungerer.
5. `git add -A && git commit -m "fix(chat): parallelisér DB-reads i chat-ruten (#164)"`

### Sted i koden
- `app/api/ai/chat/route.ts:350-373` (Promise.all + getChatHistory).
- Gå ind i `getHistory`-blokken og flyt `getChatHistory` fra linje 372 ind i Promise.all i linje 355-360.
- BEMÆRK: Der er et `latest` og `messages`-variable der bruger `history` (getChatHistory-resultatet) — du skal opfange det i Promise.all og bruge det videre.

### Constraints (fra AGENTS.md)
- Biome 2.5: `npm run check`.
- Type-check: `npx tsc --noEmit`.
- Test: `npm test -- --run --reporter=dot`.

### Vigtigt
- Koden må ikke bryde eksisterende tests.
- Gør kun ændringer der er nødvendige for at løse issue.
- `getChatHistory` skal stadig catch'e fejl (best-effort pattern).
